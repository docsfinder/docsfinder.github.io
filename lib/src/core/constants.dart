import 'package:flutter/material.dart';

class Constants {
  static const appName = 'Docs Finder';
  static const locales = [enLocale];
  static const enLocale = Locale('en');
  static const esLocale = Locale('es');
}
